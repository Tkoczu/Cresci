{
  "name": "gym-progress-tracker",
  "version": "0.1.0",
  "private": true,
  "description": "CRESCI — self-hosted gym progress tracker for Marek and Domii",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22.5"
  }
}
